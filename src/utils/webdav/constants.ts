/**
 * Constants for the WebDAV config-sync feature. Mirrors
 * `src/utils/google-drive/constants.ts` so both backends store the config
 * under the same file name.
 */

export const WEBDAV_CONFIG_FILENAME = "read-frog-config.json"
